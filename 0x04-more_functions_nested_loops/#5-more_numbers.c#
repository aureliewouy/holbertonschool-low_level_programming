#include "holberton.h"
#include <stdio.h>

/**
 * more_numbers - check the code for Holberton School students.
 *
 * Return: Always 0.
 */

void more_numbers(void)
{
	int nb;
	long int num, num2;

	for (nb = 0; nb < 10; nb++)
	{
		for (num = 0; num < 15; num++)
		{
			num2 = num;
			if (num > 9  && num < 15)
			{
				
			}
			_putchar(num2 + '0');
		}
		_putchar('\n');
	}
}
