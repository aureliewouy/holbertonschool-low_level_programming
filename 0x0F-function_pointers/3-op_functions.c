#include "3-calc.h"

/**
 * op_add - returns the sum
 * @a: the first num
 * @b: the secund num
 *
 * Return: the sum
 */

int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - returns the difference
 * @a: the first num
 * @b: the secund num
 *
 * Return: the difference int
 */

int op_sub(int a, int b)
{
	return (a - b);
}
/**
 * op_mul - returns the product of a and b
 * @a: the first num
 * @b: the secund num
 *
 * Return: the product
 */

int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - returns the division result
 * @a: the first num
 * @b: the secund num
 *
 * Return: the division.
 */

int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - the remmainder of the division
 * @a: the first num
 * @b: the secund nul
 *
 * Return: the remainder
 */

int op_mod(int a, int b)
{
	return (a % b);
}
