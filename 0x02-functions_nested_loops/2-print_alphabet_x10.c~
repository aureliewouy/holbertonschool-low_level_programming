#include "holberton.h"

void print_alphabet_x10(void)
{
	char alphabet;
	int compteur = 0;

	while (compteur < 10)
	{
		for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
		{
			_putchar(alphabet);
		}
		compteur++;
		_putchar('\n');
	}
	return;

}
