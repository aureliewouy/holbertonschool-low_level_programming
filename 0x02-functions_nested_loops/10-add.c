#include "holberton.h"
/**
 * add - the code that add two integer.
 *@a: the first integer that we add
 *@b: the second integer that we add.
 *
 * Return: Always 0.
 */

int add(int a, int b)
{
	int c = a + b;

	return (c);
}
